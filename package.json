{
  "name": "familyfoto",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx kill-port 3001 5174 && pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  }
} 